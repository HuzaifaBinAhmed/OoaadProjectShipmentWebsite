<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User List - My Application</title>

    <!-- Stylesheets -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="//cdn.datatables.net/1.10.25/css/jquery.dataTables.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha512-Fo3rlrZj/k7ujTnHg4C+6ypt4J3D5+gZlZ9Qs7JdfbRymYP0A+bIC/0mi4hd/S3eT0s3O91KOkE7ebOsBr8KFA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <style>
        /* Override DataTable default styles */
        #tblData {
            border-collapse: collapse;
            width: 100%;
            font-size: 0.875rem; /* Adjust font size */
        }

            #tblData th, #tblData td {
                border: 1px solid #dee2e6; /* Subtle border */
                padding: 0.5rem; /* Reduced padding for a cleaner look */
                overflow: hidden; /* Hide overflow content */
                text-overflow: clip; /* Add ellipsis for clipped content */
                white-space: nowrap; /* Prevent text wrapping */
            }

            #tblData thead th {
                background-color: #f1f1f1; /* Light gray background */
                color: #333; /* Darker text color */
                font-weight: normal; /* Normal font weight */
            }

            #tblData tbody tr:hover {
                background-color: #f9f9f9; /* Slightly different gray on hover */
            }

            #tblData tfoot th {
                background-color: #f1f1f1; /* Same background for footer */
            }

        /* Button Styles */
        .btn-primary {
            background-color: #007bff; /* Primary color */
            border: none;
            color: #fff;
            padding: 0.375rem 0.75rem; /* Smaller padding */
            font-size: 0.75rem; /* Smaller font size */
            border-radius: 0.375rem; /* Rounded corners */
            text-transform: uppercase; /* Uppercase text */
        }

        .btn-secondary {
            background-color: #6c757d; /* Secondary color */
            border: none;
            color: #fff;
            padding: 0.375rem 0.75rem; /* Smaller padding */
            font-size: 0.75rem; /* Smaller font size */
            border-radius: 0.375rem; /* Rounded corners */
            text-transform: uppercase; /* Uppercase text */
        }

            .btn-primary:hover, .btn-secondary:hover {
                opacity: 0.9; /* Subtle hover effect */
            }

        .table-container {
            display: flex;
            flex-direction: column;
            overflow-x: auto; /* Allow horizontal scroll if needed */
        }

        /* Card Styles */
        .card-header {
            background-color: #f1f1f1;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <!-- Header Section -->
        <div class="row mb-4 align-items-center">
            <div class="col-md-6">
                <h2 class="fw-bold text-secondary">User List</h2>
            </div>
            <div class="col-md-6 text-end">
                <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-primary rounded-pill shadow-sm">
                    <i class="bi bi-plus-circle me-1"></i> Register New User
                </a>
            </div>
        </div>

        <!-- Table Section -->
        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-table me-1"></i> User Data
            </div>
            <div class="card-body">
                <div class="table-container">
                    <table id="tblData" class="table table-bordered">
                        <thead>
                            <tr>
                                <th class="text-start">Name</th>
                                <th>Email</th>
                                <th>Phone Number</th>
                                <th>Address</th>
                                <th>Role</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <!-- Data rows go here -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="//cdn.datatables.net/1.10.25/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/user.js"></script>
</body>
</html>
